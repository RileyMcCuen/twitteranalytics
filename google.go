package main

func Analyse(tweets []string) (*AnalysedData, error) {
	data := &AnalysedData{}
	// TODO: Send the tweets to the google sentiment analyser and collect the
	//       results. Return the results in the pprovided struct.
	return data, nil
}
